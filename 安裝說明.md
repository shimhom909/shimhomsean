# 類股動能雷達 · 安裝說明

寫給不寫程式的人。全程免費、不需要租伺服器，照著做大約 40 分鐘。
完成後每個交易日早上會自動更新，你只要打開一個網址就好。

---

## 這包裡面有什麼

| 檔案 | 作用 | 你要不要動它 |
|---|---|---|
| `themes.yaml` | 20 個主題籃子的定義 | **會**，這是唯一要維護的檔案 |
| `radar.py` | 抓行情、算動能分數 | 不用 |
| `index.html` | 儀表板網頁 | 不用 |
| `.github/workflows/daily.yml` | 每日自動更新的排程 | 不用 |
| `requirements.txt` | 需要的套件清單 | 不用 |
| `data.json` | 計算結果（**目前是隨機示範資料**） | 自動產生 |
| `_test_offline.py` | 離線測試用，可刪 | 不用 |

---

## 路線一：先在自己電腦看一眼（15 分鐘，建議先做）

目的是確認資料抓得到、數字算得對，再談自動化。

**1. 裝 Python**
到 python.org 下載安裝。Windows 安裝時**務必勾選 "Add Python to PATH"**，這步漏掉後面全部會失敗。

**2. 開終端機**
Windows 按 `Win+R` 輸入 `cmd`；Mac 開「終端機」。

**3. 切到這個資料夾**
輸入 `cd ` （後面有空格），然後把資料夾直接拖進終端機視窗，按 Enter。

**4. 裝套件**（只需一次）
```
pip install -r requirements.txt
```

**5. 抓資料**
```
python radar.py
```
會跑 1–3 分鐘。看到 `✅ 已寫出 data.json` 就成功了。
過程中出現「⚠️ 資料不足或代號無效，已剔除: XXXX」是正常的——那些是已下市或改代號的股票，記下來之後從 `themes.yaml` 刪掉即可。

**6. 看儀表板**
```
python -m http.server
```
然後瀏覽器開 `http://localhost:8000`。

> 為什麼不能直接雙擊 `index.html`？瀏覽器的安全限制不允許本機網頁讀取本機檔案，必須透過這個小型伺服器。上線到 GitHub Pages 後就沒這個問題。

---

## 路線二：架上網路並自動每日更新（25 分鐘）

**1. 註冊 GitHub 帳號**（github.com，免費）

**2. 建立儲存庫**
右上角 `+` → `New repository`。名稱隨意（例如 `stock-radar`），選 **Public**（Private 也可以，但免費版 Pages 不支援）。按 `Create repository`。

**3. 上傳檔案**
在新頁面點 `uploading an existing file`，把這個資料夾裡的**所有東西**拖進去，包含隱藏的 `.github` 資料夾。

> `.github` 資料夾在 Windows/Mac 可能是隱藏的。Mac 按 `Cmd+Shift+.` 顯示隱藏檔；Windows 在檔案總管「檢視」勾選「隱藏的項目」。**這個資料夾沒傳上去，自動更新就不會運作。**

拖完按 `Commit changes`。

**4. 開啟 GitHub Pages**
儲存庫的 `Settings` → 左邊 `Pages` → `Build and deployment` 的 Source 選 **GitHub Actions**（不是 Deploy from a branch）。

**5. 開啟 Actions 並手動跑第一次**
上方 `Actions` 分頁 → 如果問你要不要啟用就按啟用 → 左邊選「每日更新動能雷達」→ 右邊 `Run workflow` → `Run workflow`。

等 3–5 分鐘，綠色勾勾出現就成功了。

**6. 打開你的網站**
網址是 `https://你的帳號.github.io/儲存庫名稱/`
（`Settings → Pages` 上方也會直接顯示這個網址。）

之後每個交易日台灣時間早上 6:10 會自動重跑，你什麼都不用做。

---

## 日常維護

**改主題或成分股**：在 GitHub 上點開 `themes.yaml` → 右上角鉛筆圖示 → 改完按 `Commit changes`。下次自動更新就會生效，也可以到 Actions 手動按 Run workflow 立刻套用。

**調整訊號敏感度**：打開 `radar.py`，最上面這幾行是全部的參數：

```python
ENTER  = 55   # 上穿此值 = 新啟動 → 調低訊號變多但雜訊也多
STRONG = 70   # 以上 = 加速
WEAK   = 45   # 以下 = 衰竭
```

以及分數的權重配方：
```python
WEIGHTS = {
    "rs20":     0.30,   # 相對大盤超額報酬
    "roc20":    0.25,   # 自身動能
    "breadth":  0.20,   # 成分股廣度
    "trend":    0.15,   # 偏離 50 日線
    "volratio": 0.10,   # 量能
}
```
加起來要等於 1。想更早偵測就提高 `volratio` 和 `breadth`；想要訊號更乾淨就提高 `rs20`。

**GitHub Actions 免費額度**：Public 儲存庫無限，Private 每月 2000 分鐘。這個工作每次約 3 分鐘、一個月約 66 分鐘，兩種情況都用不完。

---

## 常見狀況

| 症狀 | 處理 |
|---|---|
| `pip` 不是內部或外部命令 | Python 安裝時沒勾 Add to PATH，重裝一次 |
| 大量標的被剔除 | Yahoo 偶爾抽風，隔一小時再跑一次；持續失敗才是代號真的失效 |
| Actions 亮紅色叉叉 | 點進去看最後幾行紅字，把訊息貼給我 |
| 網頁顯示「讀不到 data.json」 | 本機用的話要先跑 `python -m http.server` |
| 網頁還是舊資料 | 按 `Ctrl+Shift+R`（Mac `Cmd+Shift+R`）強制重新整理 |

---

## 三個必須知道的限制

**這不是預測工具。** 動能是同時／落後指標，它偵測的是「已經開始動」。這套系統的價值在於把你發現的時間從 4 週壓到 1–2 週，不是讓你買在起漲點。

**分數是相對的，不是絕對的。** 80 分的意思是「今天在這 20 個主題裡排前面」，不是「這個主題很強」。空頭市場裡照樣會有 90 分的主題——它只是跌得比較少。所以務必同時看「避險」分頁：當成長組集體衰竭、貴金屬與公用事業同時上升，那個交叉才是有意義的市場訊號。

**還沒有回測。** 目前這版只跑當下的訊號，沒有驗證過去三年這些規則實際賺不賺錢。在投入真錢之前，這一步不能跳過——而且回測要特別小心倖存者偏誤：現在 `themes.yaml` 裡的成分股是「今天看起來屬於這個主題」的公司，用它去回測三年前會嚴重高估績效。

---

# 追加模組：內部人交易與融券餘額

版本 2 新增 `flows.py`，抓兩類「價格還沒動就先出現」的資料。

## 這兩個數字怎麼看

**內部人淨買賣（−100 ～ +100）**
公司董事、高層、10% 大股東在公開市場買賣自家股票的金額比。+100 代表全是買進，−100 全是賣出。

只採計交易代碼 **P（公開市場買進）** 和 **S（賣出）**，刻意排除配股（A）、選擇權行使（M）、扣稅賣出（F）——那些是薪酬制度的自動結果，不是主動判斷，沒有訊號價值。這是這個指標好不好用的關鍵，很多免費工具沒做這層過濾，數字會失真。

解讀時要注意**買賣不對稱**：內部人賣出的理由很多（買房、繳稅、分散風險），訊號很弱；**買進基本上只有一個理由**，訊號強得多。所以 +30 比 −30 有意義得多。

**融券占流通股比例（%）與月變化**
空單押注的規模。括號裡的百分比是較上月的變化。

高融券有兩種完全相反的讀法：可能是機構看空（利空），也可能是軋空燃料（一旦轉強會漲得更兇）。**單看沒有方向性**，要跟動能分數一起看：高融券 + 動能剛啟動 = 軋空條件；高融券 + 動能衰竭 = 空方正確。

## 執行方式

```
python flows.py
```

第一次跑要 **5～15 分鐘**（要向 SEC 查詢一百多家公司）。跑完會產生 `flows.json`。之後再跑一次 `python radar.py`，資料就會併進儀表板。

以後的順序是：`python flows.py` → `python radar.py` → 開網頁。

**開跑前先改一個地方**：打開 `themes.yaml`，最上面這行改成你自己的 email：
```yaml
sec_user_agent: "Personal Research your-email@example.com"
```
SEC 規定抓取資料時必須表明身分。不改也能跑，但可能被擋。

## 自動排程

已經寫進 `daily.yml`，**每週二**自動跑一次，其他天沿用快取結果。

為什麼不每天？Form 4 規定兩個工作天內申報、融券每兩週才公布一次，每天抓不會有新東西，只會拖慢排程。這步設定了 `continue-on-error`，就算 SEC 當機也不會中斷你的動能更新。

## 儀表板新增什麼

每張卡片下方多一列，顯示內部人淨買賣與融券。上方多一個**排序選單**，可以改用「內部人淨買入」或「融券月增幅」排序——這是找出「價格還沒動、但內部人已經在買」的用法。

抓不到資料的主題會顯示「—」，不影響其他部分。

## 已知限制

**內部人資料有申報延遲。** 法規要求交易後兩個工作日內申報，所以你看到的最快也是兩天前的事。這仍然比價格動能早，但不是即時。

**融券是快照不是歷史。** 目前用的來源只給當下值和上月值，做不出趨勢圖。要完整歷史得改接 FINRA 的原始檔案。

**小型股的內部人資料很稀疏。** 量子運算、太空這類主題裡有些公司幾個月都沒有一筆 P 或 S，那欄就會是「—」，這是正常的，不是程式壞了。

**這些都不是預測。** 內部人也會看錯，機構的大額空單也常常是避險部位而非看空押注。這些指標的作用是多一個角度交叉驗證，不是新的水晶球。

---

# 追加模組：台廠供應鏈 → 美股推估

## 這個模組在做什麼

台灣上市櫃公司依法規每月 10 日前公布**上月**營收，美股是**季報**。
台廠 6 月營收在 7 月 10 日就看得到，但下游美廠涵蓋 6 月的那一季財報，
要等到 8 月甚至 9 月才公布。這中間的 **3 週到 2 個月**，就是這個模組要抓的落差。

## 執行方式

```
python twrev.py
```

跑完會產生 `twchain.json`，再跑 `python radar.py`，然後開網頁，
上方會多一個「**台廠供應鏈推估**」分頁。

自動排程已設定為**每月 11–15 日**執行（台灣 10 日截止公布，11 日之後才有新資料；
抓五天的區間是容錯，某天失敗還有機會補上）。

## 怎麼讀這個頁面

**讀數（%）**
對應台廠月營收年增率的加權平均。權重 = 相關性 × 純度，兩者都在
`supply_chain.yaml` 裡，你可以自己調。

**信心度（0–100）— 這是最重要的欄位**
綜合三件事：供應商家數、彼此方向是否一致、整體純度。

**信心度低的讀數請直接忽略。** 例如 ALAB 只有世芯一家對應，
就算讀數 +95% 也只代表世芯這一家的狀況，不代表 ALAB 的需求。
相對地 SMCI 有緯穎、廣達、緯創三家互相印證，讀數才有參考價值。

**加速度（pp）**
3 個月滾動年增率比上一期高／低幾個百分點。**這一欄比讀數本身更有用**——
年增率從 +80% 掉到 +50% 仍是高成長，但動能其實正在轉弱。
這欄需要累積約 15 期歷史才會完整，第一年會是空的。

## 為什麼歷史要自己累積

TWSE OpenAPI 只回傳**最新一期**快照，沒有查詢歷史的參數。所以：

- **第一次跑就有**：年增率、月增率（快照本身含當月／上月／去年同月三個數字）
- **累積 3 期後有**：3 個月滾動年增率
- **累積 15 期後有**：加速度指標完整可用

`twrev_history.json` 就是累積檔，會自動保留最近 36 期。**不要刪掉它**，
刪了要重新累積一年多。GitHub Actions 那邊已經設定快取保存。

## 修改對應表

打開 `supply_chain.yaml`：

```yaml
  "6669":
    name: 緯穎
    market: twse
    purity: 0.85        # 這家有多少營收來自 AI（0–1，寧可保守）
    maps_to: {NVDA: 3, SMCI: 2}    # 對應美股與相關性 1–3
```

`purity` 憑你的產業判斷填即可，填保守一點不會害你——它只會壓低信心度，
不會製造假訊號。

---

## 使用前必須理解的五個限制

這個模組的邏輯是成立的，但有幾個地方很容易讓人得出過度自信的結論。

**一、台積電營收不是資訊優勢。**
它每月 10 日公布，全球分析師都在看，NVDA 幾分鐘內就反應完了。
真正的優勢在**較少人盯的中型供應商**——台光電、欣興、南電這類。
系統照樣會顯示台積電，但別把它當成獨家資訊。

**二、歸因問題是最大的弱點。**
月營收**沒有部門別拆解**。廣達的營收同時包含筆電和 AI 伺服器，
營收跳增可能是筆電旺季，跟 AI 無關。`purity` 這個參數就是為了部分修正這件事，
但它是你的估計，不是事實。這是整套方法最脆弱的一環。

**三、三個常見的假訊號來源：**
- **匯率**：營收以新台幣計價，台幣貶值會自動墊高營收，跟實際出貨量無關
- **農曆年**：一、二月的年增率完全失真，必須把 1+2 月合併看
- **拉貨與塞貨**：季底拉貨、通路庫存堆積都會美化營收，但不等於終端需求

**四、時間差是真的，但沒有想像中大。**
台廠 M 月營收在 M+10 天公布，美廠涵蓋 M 月的季報在 M+30～70 天公布。
實際領先 3 週到 2 個月，是真實優勢，但不是幾個月。

**五、上櫃公司目前抓不到。**
官方 API 只有上市（TWSE）的月營收端點，上櫃（TPEx）沒有對應端點。
這會漏掉**信驊、譜瑞、穩懋、群聯、環球晶、世界先進**六家，
其中信驊（BMC 晶片，幾乎每台伺服器一顆）是很好的伺服器出貨代理指標，
漏掉可惜。程式會清楚列出哪幾家因此缺資料，不會靜默失敗。

要補這塊得改用 MOPS 網頁版或第三方付費源，之後可以再處理。

---

**最後**：這個模組輸出的是「供應鏈狀況觀察」與推論假設，不是投資建議。
它告訴你上游在發生什麼事，不告訴你該不該買。營收好不代表股價會漲——
市場可能早就反映了，也可能在意的是毛利率而不是營收。
